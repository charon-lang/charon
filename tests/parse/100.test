root
(root)
  (tlc.function `hello` `fn(): void`)
    (stmt.block)
  (tlc.function `world` `fn(): uint64`)
    (stmt.block)
  (tlc.function `hello_world` `fn(uint64, uint8, ...): uint64`)
    args:
      - a
      - b
    (stmt.block)