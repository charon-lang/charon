root
(root)
  (tlc.function `hello` `fn(): void`)
    (stmt.block)
      (stmt.declaration `x`)
        (expr.literal_numeric `5`)
      (stmt.expression)
        (expr.binary `-`)
          (expr.binary `+`)
            (expr.literal_numeric `5`)
            (expr.literal_numeric `5`)
          (expr.variable `x`)
      (stmt.noop)
      (stmt.noop)
      (stmt.noop)
      (stmt.noop)
      (stmt.noop)