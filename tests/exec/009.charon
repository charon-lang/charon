extern fn printf(fmt: *u8, ...): i32;

fn main() {
    let a: u64;
    let b: u64;
    let c: u64;
    let d: u64;

    a = b = c = d = 5965;

    printf("> %lu\n", a);
    printf("> %lu\n", b);
    printf("> %lu\n", c);
    printf("> %lu\n", d);
}