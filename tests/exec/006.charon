extern fn printf(fmt: *u8, ...): i32;

fn x(): uint {
    return 5;
}

fn add(a: uint, b: uint): uint {
    return a + b;
}

fn z() {
    return;
}

fn main() {
    printf("> %lu\n", x());
    printf("> %lu\n", add(5, 10));
    z();
}