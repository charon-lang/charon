extern u32 printf(u8 *fmt, ...);
extern void *malloc(uint size);
extern void free(void *ptr);

u64 main() {
    void *p = malloc(100);
    uint *x = p;
    *x = 5;

    *x *= 10;
    printf("%p %lu\n", p, *x);
    free(p);
    return 0;
}